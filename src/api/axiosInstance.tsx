// Re-export the configured axios instance
export { default } from "./axiosInstance";

// Re-export the setup function for refresh token interceptors
export { setupAxiosInterceptors } from "./refreshToken";
